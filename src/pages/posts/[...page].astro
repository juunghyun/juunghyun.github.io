---
import type { GetStaticPaths } from "astro";
import PostListPage from "@/components/pages/PostListPage.astro";
import { getPostListPaths } from "@/utils/routes";

export const getStaticPaths = (async options =>
  getPostListPaths("ko", options)) satisfies GetStaticPaths;
---

<PostListPage page={Astro.props.page} />
